# Get your key from https://console.anthropic.com/
ANTHROPIC_API_KEY="sk-ant-..."

# GitHub personal access token — used by /api/fetch-repo to raise the API rate limit
# Public repos only. Read-only scope is sufficient.
GITHUB_TOKEN="ghp_..."
